<?xml version="1.0" encoding="utf-8"?>



<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Efecto ripple al tocar -->

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/c100" /><!-- Color de fondo marrón -->
            <corners android:radius="20dp" /> <!-- Bordes redondeados -->
            <stroke
                android:width="3dp"
                android:color="@color/c900" />  <!-- Color del borde marrón -->
        </shape>
    </item>
</ripple>
